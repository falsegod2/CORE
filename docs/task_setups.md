# MineDojo task setup

Each task entry defines simulator settings, MineCLIP prompts, sparse environment reward, success/terminal conditions, screenshots, and the standard agent wrapper.

The active agent observation is RGB-only. No task-conditioned auxiliary image field or associated checkpoint is constructed by the environment.
