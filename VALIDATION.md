# Validation

Static syntax/config checks and synthetic encoder/world-model tests are included.
A full MineDojo run was not possible in the artifact environment because the
Minecraft/MineDojo/MineCLIP runtime is unavailable.
