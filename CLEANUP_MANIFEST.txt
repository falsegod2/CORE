Active method:
RGB DreamerV3 + MineCLIP reward + frozen task/video-semantic-conditioned object tokens.

Semantic inputs:
- task_embedding: frozen MineCLIP text feature
- mineclip_embedding: frozen global video feature reused from reward scoring
- neither feature is decoded or optimized by RL gradients

Absent from the active training path:
- heatmap / affordance map input
- affordance intrinsic reward
- zoomed image or long-term jump
- S/Z split and inverse dynamics
- pure unsupervised Slot Attention
- direct global MineCLIP-to-RSSM concatenation

Object-token implementation:
- task/state semantic candidate selection
- task/state-conditioned object queries
- task-relevant scene token
- coverage, diversity, and semantic-alignment regularization
- conservative residual injection into the unchanged RGB token grid
